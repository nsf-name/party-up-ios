// SendScreen.swift
import SwiftUI

struct SendScreen: View {
    var body: some View {
        NavigationStack {
            Group {
                VStack {
                    Divider()
                    Text("TODO: finish this view")
                        .padding([.leading,.trailing],16)
                    Spacer()
                }
            }
            .navigationTitle(Text("Send Files"))
        }
    }
}

